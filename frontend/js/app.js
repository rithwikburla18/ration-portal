const API = window.RATION_API_BASE || "http://localhost:8080/api"; function trackCard() { const number = document.getElementById("rationNumber").value.trim(); const result = document.getElementById("trackResult"); if (!number) { result.textContent = "Please enter a ration card number."; return; } result.textContent = "Searching..."; fetch(`${API}/ration-cards/${encodeURIComponent(number)}`).then(response => { if (!response.ok) throw new Error("not-found"); return response.json(); }).then(card => { result.innerHTML = `<div class="result-card"><h3>Ration Card Details</h3><p><strong>Card Number:</strong> ${card.rationCardNumber || "N/A"}</p><p><strong>Card Type:</strong> ${card.cardType || "N/A"}</p><p><strong>Head of Family:</strong> ${card.headOfFamily || "N/A"}</p><p><strong>District:</strong> ${card.district || "N/A"}</p><p><strong>State:</strong> ${card.state || "N/A"}</p><p><strong>Status:</strong> ${card.status || "N/A"}</p></div>`; }).catch(error => { result.textContent = error.message === "not-found" ? "Ration card not found." : "Backend is unavailable. Please start the Spring Boot server."; }); }
